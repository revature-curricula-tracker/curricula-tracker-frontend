export class Technology {
  techId: number;
  techName: string;
  color: string;

  constructor( id: number, techName: string, color: string) {
    this.techId=id;
    this.techName=techName;
    this.color=color;
  }
}
